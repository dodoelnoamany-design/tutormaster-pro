# 📤 دليل الملفات المطلوبة للرفع على موقع

## 🎯 الملفات التي تحتاج لرفعها فقط:

### ✅ المجلدات:

```
📁 src/
   ├── App.tsx
   ├── index.tsx
   ├── index.css
   ├── types.ts
   ├── store.tsx
   ├── schoolStore.tsx
   ├── themeStore.tsx
   └── components/
       ├── AppointmentsSchedule.tsx
       ├── BottomNav.tsx
       ├── Dashboard.tsx
       ├── DailySummaryModal.tsx
       ├── FinanceReport.tsx
       ├── Header.tsx
       ├── SchoolSchedule.tsx
       ├── SessionList.tsx
       ├── StudentList.tsx
       └── Settings.tsx

📁 public/
   ├── favicon.ico
   └── (أي أيقونات أخرى)
```

### ✅ الملفات الرئيسية:

```
📄 package.json
📄 package-lock.json (اختياري)
📄 tsconfig.json
📄 vite.config.ts
📄 index.html
📄 manifest.json
📄 sw.js
📄 capacitor.config.ts (جديد!)
```

### ✅ ملفات إضافية (اختيارية):

```
📄 .gitignore
📄 .env (إن وجد)
📄 BUILD_APK_GUIDE.md
📄 FILES_FOR_UPLOAD.md
```

---

## ❌ ملفات لا تحتاج لرفعها:

```
❌ node_modules/           (ضخم: 500MB+)
❌ dist/                   (سيتم إنشاؤه)
❌ android/                (سيتم إنشاؤه)
❌ .git/                   (اختياري)
❌ build-apk.bat          (محلي فقط)
❌ build-apk.sh           (محلي فقط)
❌ .env.local             (حساس)
```

---

## 🔧 إجراء الرفع (خطوة بخطوة):

### 1️⃣ **على موقع يدعم Node.js (مثل Vercel, Netlify):**

**Vercel:**
```bash
npm install -g vercel
vercel
```
ثم اختر:
- Project Name: `tutormaster-pro`
- Framework: `Vite`
- Build command: `npm run build`
- Output directory: `dist`

**Netlify:**
```bash
npm install -g netlify-cli
netlify deploy
```

### 2️⃣ **على خادم عادي (cPanel, etc):**

```
1. ارفع الملفات أعلاه بـ FTP
2. اتصل بخادمك عبر SSH
3. شغّل الأوامر:

npm install
npm run build

4. أشِر النطاق على مجلد `dist/`
```

### 3️⃣ **على GitHub Pages (مجاني):**

```bash
# في ملف package.json، أضف:
"homepage": "https://your-username.github.io/tutormaster-pro"

# في vite.config.ts، أضف:
base: '/tutormaster-pro/'

# ثم رفع:
git add .
git commit -m "Initial commit"
git push origin main
```

---

## 📋 قائمة التحقق قبل الرفع:

```javascript
قبل الرفع على أي موقع:

☑️  تشغيل محلياً بنجاح: npm run dev
☑️  البناء بدون أخطاء: npm run build
☑️  وجود dist/ والملفات فيه
☑️  اختبار Service Worker: يعمل أوفلاين
☑️  البيانات تُحفظ محلياً (localStorage)
☑️  manifest.json موجود وصحيح
☑️  sw.js موجود وتم تحميله
☑️  جميع الشاشات تعمل (Appointments, Finance, School, etc)
☑️  الإشعارات تعمل
☑️  الألوان المخصصة تعمل
☑️  الزوم يعمل من 10% إلى 200%
```

---

## 🌐 الموقع المثالي لرفع المشروع:

### ✅ الخيار 1: Vercel (الأفضل)
- مجاني
- سريع جداً
- دعم كامل لـ Capacitor
- عملية رفع سهلة جداً

### ✅ الخيار 2: Netlify
- مجاني
- سهل الاستخدام
- preview جيد

### ✅ الخيار 3: GitHub Pages
- مجاني
- مفتوح المصدر
- يحتاج خطوات إضافية

### ✅ الخيار 4: خادم خاص
- تحكم كامل
- قد يكون مدفوع
- يحتاج معرفة تقنية

---

## 🚀 بعد الرفع على الموقع:

### لبناء APK من الموقع:

1. **اذهب إلى:** https://www.pwabuilder.com/
2. **أدخل رابط موقعك:** https://tutormaster.vercel.app
3. **اضغط:** "Package your PWA"
4. **اختر:** Android
5. **حمّل:** APK

---

## 📊 مثال العملية الكاملة:

```
Step 1: تحضير المشروع محلياً
├── npm install ✅
├── npm run build ✅
└── اختبار dist/ ✅

Step 2: رفع على Vercel
├── npm install -g vercel ✅
├── vercel ✅
└── هاه لديك رابط: https://tutormaster.vercel.app ✅

Step 3: بناء APK من PWA Builder
├── https://pwabuilder.com ✅
├── إدراج الرابط ✅
└── تحميل APK ✅

Step 4: تثبيت على الهاتف
└── نقل APK وتثبيت ✅
```

---

## 💾 البيانات آمنة 100%:

```javascript
جميع البيانات تُخزّن محلياً:
✓ localStorage (مشفر محلياً)
✓ Service Worker (محلي)
✓ لا توجد حسابات سحابية مطلوبة
✓ لا توجد متابعة خارجية
✓ لا توجد إرسال بيانات
```

---

## 🎯 الملفات النهائية للرفع:

```
upload.zip
├── src/              (كامل)
├── public/           (كامل)
├── package.json      ✅
├── tsconfig.json     ✅
├── vite.config.ts    ✅
├── index.html        ✅
├── manifest.json     ✅
├── sw.js             ✅
└── capacitor.config.ts ✅
```

**الحجم الكلي:** ~3 MB (بدون node_modules)

---

**الآن أنت جاهز للرفع والبناء!** 🎉
