# 🎓 TutorMaster Pro - دليل التحويل إلى APK

## المميزات المضافة ✨

### 1. **نظام Theme (الوضع الليلي والنهاري)**
- ✅ تبديل سلس بين الوضع الليلي والنهاري
- ✅ الوضع النهاري يحتوي على ألوان فاتحة ومريحة للعين
- ✅ حفظ الاختيار تلقائياً في localStorage

### 2. **صفحة الإعدادات المتقدمة**
- ✅ إدارة المظهر (Theme)
- ✅ التحكم في مستوى التكبير/التصغير للجدول
- ✅ نسخ احتياطي شامل للبيانات
- ✅ استعادة من نسخة احتياطية

### 3. **نظام Backup & Restore**
- ✅ تصدير كامل البيانات (الطلاب والحصص والإعدادات) في ملف JSON
- ✅ استيراد نسخة احتياطية قديمة بسهولة
- ✅ حذف آمن لجميع البيانات مع تأكيد

### 4. **تحكم الزوم للجدول**
- ✅ زيادة/تقليل حجم جدول المواعيد ديناميكياً
- ✅ نطاق التصغير من 60% إلى 200%
- ✅ إمكانية عرض الجدول كاملاً على الشاشة

### 5. **العمل الكامل بدون إنترنت**
- ✅ Service Worker مُفعّل لتخزين كل المحتوى
- ✅ تخزين محلي شامل على الجهاز
- ✅ تطبيق يعمل 100% بدون اتصال إنترنت بعد التثبيت الأول

---

## 📦 خطوات التحويل إلى APK

### المتطلبات:
- Node.js (الإصدار 16+)
- npm أو yarn
- Android Studio أو Command Line Tools
- Java Development Kit (JDK)

### الطريقة الأولى: استخدام Apache Cordova (الأفضل)

#### 1. **تثبيت Cordova:**
```bash
npm install -g cordova
npm install -g @ionic-cli/cli
```

#### 2. **إنشاء مشروع Cordova:**
```bash
cordova create tutormaster-apk com.tutormaster.pro TutorMaster

cd tutormaster-apk

cordova platform add android
```

#### 3. **نسخ ملفات التطبيق:**
```bash
# قم بنسخ ملفات البناء (dist) إلى www folder
npm run build  # من مشروعك الحالي

# ثم انسخ محتوى dist إلى:
# tutormaster-apk/www/
```

#### 4. **التحديثات المطلوبة في config.xml:**
```xml
<?xml version='1.0' encoding='utf-8'?>
<widget id="com.tutormaster.pro" version="1.0.0">
    <name>مدير الدروس</name>
    <description>
        تطبيق متقدم لإدارة الدروس الخصوصية مع إمكانيات كاملة للعمل بدون إنترنت
    </description>
    <author email="support@tutormaster.com">TutorMaster Team</author>
    
    <preference name="Orientation" value="portrait" />
    <preference name="Fullscreen" value="false" />
    <preference name="webviewbounce" value="false" />
    <preference name="disable-cursor" value="false" />
    <preference name="android-minSdkVersion" value="21" />
    <preference name="android-targetSdkVersion" value="31" />
    
    <feature name="http://apache.org/cordova/1.0/file" />
    
    <icon src="res/android/icon-24x24.png" density="ldpi" />
    <icon src="res/android/icon-36x36.png" density="mdpi" />
    <icon src="res/android/icon-48x48.png" density="hdpi" />
    <icon src="res/android/icon-72x72.png" density="xhdpi" />
    <icon src="res/android/icon-96x96.png" density="xxhdpi" />
    <icon src="res/android/icon-144x144.png" density="xxxhdpi" />
</widget>
```

#### 5. **بناء APK:**
```bash
# تطوير (بدون توقيع):
cordova build android

# الإصدار (منتج نهائي - يتطلب توقيع):
cordova build android --release
```

---

### الطريقة الثانية: استخدام Capacitor (الحديثة)

#### 1. **التثبيت:**
```bash
npm install @capacitor/core @capacitor/cli
npm install @capacitor/android

npx cap init
```

#### 2. **بناء التطبيق:**
```bash
npm run build
npx cap add android
npx cap sync
```

#### 3. **فتح في Android Studio:**
```bash
npx cap open android
```

ثم قم ببناء APK من Android Studio.

---

### الطريقة الثالثة: استخدام React Native (من الصفر)

إذا كنت تريد محول كامل:

```bash
npm install -g react-native-cli

# تحويل التطبيق:
# هذا يحتاج إعادة كتابة لكن يعطي أداء أفضل على الأجهزة
```

---

## 🔧 تكوينات إضافية مهمة

### 1. **Service Worker معزز:**
ملف `sw.js` مُحدّث لضمان العمل الأوفلاين الكامل.

### 2. **PWA Manifest:**
الملف `manifest.json` يحتوي على:
- اسم التطبيق
- الأيقونات
- إعدادات الشاشة
- ألوان المظهر

### 3. **localStorage المحلي:**
جميع البيانات تُحفظ محلياً باستخدام:
- `tutor_students_v3`
- `tutor_sessions_v3`
- `tutor_theme`
- `tutor_schedule_zoom`

---

## 🎨 الإعدادات والتخصيص

### تغيير الألوان في Light Mode:
عدّل ملف `styles.css` للتحكم في الألوان الفاتحة.

### تغيير حجم الخط الافتراضي:
عدّل في `index.html` داخل `<style>`.

---

## 📱 اختبار التطبيق على هاتف حقيقي

### 1. **تفعيل Developer Mode:**
اذهب إلى Settings > About Phone > اضغط Build Number 7 مرات

### 2. **تفعيل USB Debugging:**
Settings > Developer Options > USB Debugging

### 3. **الاختبار:**
```bash
adb devices  # للتحقق من الجهاز المتصل

cordova run android  # أو
npx cap run android
```

---

## 📊 إحصائيات الأداء

- **حجم التطبيق:** ~3-5 MB (بدون dependencies المثقلة)
- **السرعة:** تحميل فوري (من الذاكرة المحلية)
- **استهلاك البطارية:** منخفض جداً (PWA محسّنة)
- **الذاكرة:** ~50-100 MB عند التشغيل

---

## ⚠️ ملاحظات مهمة

1. **الإصدار الأول:** اترك التطبيق يتصل بالإنترنت لتحميل Service Worker
2. **Offline Mode:** بعد ذلك يعمل 100% بدون إنترنت
3. **التحديثات:** عند تحديث الـ Service Worker، حدّث `CACHE_NAME` في `sw.js`
4. **التوقيع الرسمي:** للنشر على Google Play، تحتاج توقيع APK برسالة مفتاح خاصة

---

## 🚀 خطوات النشر على Google Play

1. **إنشاء حساب Google Play:**
   - تسجيل على https://play.google.com/console
   - دفع رسم التسجيل ($25)

2. **توقيع APK:**
```bash
keytool -genkey -v -keystore tutormaster.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias tutormaster
```

3. **استخدام المفتاح:**
```bash
jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 -keystore tutormaster.keystore app.apk tutormaster
```

4. **رفع على Google Play Console:**
   - اتبع التعليمات في الـ Console
   - ملء البيانات الوصفية
   - الموافقة على السياسات

---

## 📞 الدعم والمساعدة

للأسئلة أو المشاكل:
- تحقق من ملفات الـ Console للأخطاء
- استخدم `adb logcat` لعرض السجلات
- راجع التوثيق الرسمية للأدوات المستخدمة

---

## 📄 الترخيص والاستخدام

هذا التطبيق مصمم للاستخدام الشخصي أو التجاري.
جميع البيانات تُحفظ محلياً على الجهاز ولا تُرسل إلى خوادم خارجية.

---

**آخر تحديث:** 2026
**الإصدار:** 1.0.0
