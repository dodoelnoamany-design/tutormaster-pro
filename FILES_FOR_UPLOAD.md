# 📋 ملفات المشروع للرفع (Build Checklist)

## ✅ الملفات المطلوبة بالفعل:

### 1️⃣ **ملفات المصدر:**
```
src/
├── App.tsx              ✅ مهم جداً
├── index.tsx            ✅ نقطة الدخول
├── types.ts             ✅ تعريفات TypeScript
├── store.tsx            ✅ إدارة الحالة (Students/Sessions)
├── schoolStore.tsx      ✅ إدارة جدول المدرسة
├── themeStore.tsx       ✅ الإعدادات والألوان
└── components/
    ├── AppointmentsSchedule.tsx   ✅
    ├── BottomNav.tsx              ✅
    ├── Dashboard.tsx              ✅
    ├── DailySummaryModal.tsx       ✅
    ├── FinanceReport.tsx           ✅
    ├── Header.tsx                  ✅
    ├── SchoolSchedule.tsx          ✅
    ├── SessionList.tsx             ✅
    ├── StudentList.tsx             ✅
    └── Settings.tsx                ✅
```

### 2️⃣ **ملفات الإعدادات:**
```
✅ package.json         (المتطلبات والنصوص)
✅ tsconfig.json        (إعدادات TypeScript)
✅ vite.config.ts       (إعدادات البناء)
✅ index.html           (ملف HTML الرئيسي)
```

### 3️⃣ **ملفات الويب:**
```
✅ manifest.json        (معلومات PWA)
✅ sw.js                (Service Worker - (للعمل بلا إنترنت)
✅ favicon.ico          (أيقونة المتصفح)
```

### 4️⃣ **ملفات Capacitor (جديدة):**
```
✅ capacitor.config.ts  (إعدادات Capacitor)
```

### 5️⃣ **ملفات البناء:**
```
dist/                   (سيتم إنشاؤه تلقائياً من npm run build)
├── index.html
├── assets/
│   ├── *.js
│   └── *.css
└── ...
```

---

## ❌ ملفات لا تحتاج للرفع:

```
❌ node_modules/        (ضخم جداً - 500MB+)
❌ .git/               (ليس ضروري)
❌ android/            (سيتم إنشاؤه من npx cap add android)
❌ build-apk.bat       (ملف محلي فقط)
❌ build-apk.sh        (ملف محلي فقط)
```

---

## 🔄 خطوات البناء الكاملة:

```bash
# 1️⃣ تثبيت المتطلبات
npm install

# 2️⃣ بناء المشروع
npm run build

# 3️⃣ تثبيت Capacitor
npm install @capacitor/core @capacitor/cli

# 4️⃣ إنشاء ملف الإعدادات
npx cap init "مدير الدروس" "com.tutormaster.pro" --web-dir dist

# 5️⃣ إضافة منصة Android
npx cap add android

# 6️⃣ مزامنة الملفات
npx cap sync android

# 7️⃣ بناء APK
cd android
./gradlew assembleDebug
```

---

## 📁 هيكل المشروع النهائي:

```
tutormaster-pro/
├── src/                 ✅
├── public/              ✅
├── dist/                ✅ (من npm run build)
├── android/             ✅ (من npx cap add android)
├── node_modules/        (تاجهل بعد البناء)
├── package.json         ✅
├── vite.config.ts       ✅
├── capacitor.config.ts  ✅
├── tsconfig.json        ✅
├── manifest.json        ✅
├── sw.js                ✅
├── index.html           ✅
└── BUILD_APK_GUIDE.md   📖
```

---

## 🌐 لو كنت تريد رفع على موقع خارجي:

**الملفات والمجلدات المطلوبة فقط:**

```
📦 upload/
├── 📁 src/
├── 📁 public/
├── 📁 dist/          (اختياري - سيتم إنشاؤه)
├── 📄 package.json
├── 📄 tsconfig.json
├── 📄 vite.config.ts
├── 📄 capacitor.config.ts
├── 📄 index.html
├── 📄 manifest.json
├── 📄 sw.js
└── 📄 .gitignore     (اختياري)
```

**الحجم الكلي:** ~2-5 MB (بدون node_modules)

---

## ✨ نصائح مهمة:

1. **قبل الرفع على موقع:**
   - تأكد من `npm run build` يعمل بنجاح
   - تحقق من أن `dist/` موجود وينتشر الملفات صحيحة

2. **قبل البناء:**
   - تأكد من `java -version` يعطيك رقم نسخة صحيح
   - تأكد من `npm -v` يعطيك رقم نسخة صحيح

3. **بعد البناء:**
   - الملف النهائي: `android/app/build/outputs/apk/debug/app-debug.apk`
   - الحجم: ~15-20 MB
   - يعمل 100% بدون إنترنت

---

## 🎯 الملف الأساسي الذي تحتاجه:

**إذا كان لديك رابط موقع فقط (PWA Builder):**
- كل ما تحتاجه هو `npm run build` و رابط الموقع

**إذا كنت تبني محلياً (Capacitor):**
- كل الملفات أعلاه

---

**الآن أنت جاهز للبناء!** 🚀
